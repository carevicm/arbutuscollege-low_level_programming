#include <stdio.h>
#include <unistd.h>

/**
 * main -Entry point
 * Return: Always 1 (Success)
 */
int main(void)
{
	fwrite(2, "and that piece of art is usefu - Dora Korpar, 2015-10-19\n", 58);
	return (1);
}
